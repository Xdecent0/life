// Форма состояния, в котором ещё ничего не прочитано.
//
// `board` пустой — это «снимок ещё не приезжал», а не «проектов нет». Разница
// видна на экране: приложение, которое не синкалось, так и говорит.

export { size, isFresh, load, save, reset } from "../../../core/store.js";

export const EMPTY_STATE = {
  version: 1,

  /** Снимок доски: собран на компьютере из заметок волта, читается как есть. */
  board: null,

  /** Единственное, что приложение пишет: «закрой веху», «дело сделано». */
  edits: [],

  queue: [],
  syncedAt: null,
};
